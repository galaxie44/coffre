name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - name: Build Windows
        working-directory: apps/coffre
        run: |
          flutter pub get
          flutter build windows --release
      - name: Stage dist
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist/windows/Coffre | Out-Null
          Copy-Item -Recurse -Force apps/coffre/build/windows/x64/runner/Release/* dist/windows/Coffre/
          if (Test-Path apps/coffre/assets/icon/coffre.ico) {
            Copy-Item apps/coffre/assets/icon/coffre.ico dist/windows/Coffre/Coffre.ico -Force
          }
      - name: Install Inno Setup
        run: choco install innosetup -y
      - name: Compile installer
        shell: pwsh
        run: |
          $iscc = @(
            "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe",
            "$env:ProgramFiles\Inno Setup 6\ISCC.exe",
            "$env:LOCALAPPDATA\Programs\Inno Setup 6\ISCC.exe"
          ) | Where-Object { Test-Path $_ } | Select-Object -First 1
          if (-not $iscc) { throw "ISCC.exe introuvable" }
          & $iscc installer\windows\coffre.iss
          & $iscc installer\windows\coffre_wipe.iss
      - uses: actions/upload-artifact@v4
        with:
          name: windows-setup
          path: |
            installer/windows/Output/Coffre-Setup-Windows.exe
            installer/windows/Output/Coffre-Supprimer-Tout.exe

  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - name: Build APK
        working-directory: apps/coffre
        run: |
          flutter pub get
          flutter build apk --release
      - uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: apps/coffre/build/app/outputs/flutter-apk/app-release.apk

  publish:
    needs: [windows, android]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
      - name: Prepare assets
        run: |
          mkdir -p dist/release
          cp windows-setup/Coffre-Setup-Windows.exe dist/release/
          cp windows-setup/Coffre-Supprimer-Tout.exe dist/release/
          cp android-apk/app-release.apk dist/release/Coffre.apk
      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/release/Coffre-Setup-Windows.exe
            dist/release/Coffre-Supprimer-Tout.exe
            dist/release/Coffre.apk
          generate_release_notes: true
          body: |
            ## Téléchargement
            - **Windows** : lancez `Coffre-Setup-Windows.exe`
            - **Android** : ouvrez `Coffre.apk` sur le téléphone
            - **Tout supprimer (Windows)** : `Coffre-Supprimer-Tout.exe` (irréversible)
